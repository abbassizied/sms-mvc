package io.github.abbassizied.sms.enums;

/**
 * Enum representing different types of notifications in the system.
 */
public enum NotificationType {
    
    RECEIVED_CONTACT_MESSAGE,   // Notification for receiving a contact message
    RECEIVED_PRIVATE_MESSAGE,   // Notification for receiving a private message
    NEW_ORDER                   // Notification for a new order event
}